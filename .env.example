# Copy to .env and fill in. For Docker Compose, DATABASE_URL is set automatically
# when app runs inside compose (host=postgres). For local dev with only DB in Docker:
# DATABASE_URL=postgresql://postgres:Candy@localhost:5432/english-learn

DATABASE_URL=postgresql://postgres:Candy@postgres:5432/english-learn
AUTH_TOKEN_TTL_DAYS=30
